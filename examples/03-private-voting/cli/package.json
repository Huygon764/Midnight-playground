{
  "name": "@example/private-voting-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "tsx src/index.ts",
    "build": "rm -rf dist && tsc --project tsconfig.json"
  },
  "dependencies": {
    "@example/private-voting-contract": "*"
  }
}
